[project]
name = "skating-map-scripts"
version = "0.1.0"
description = "Scripts to manage skating routes data"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.28.0",
]

[dependency-groups]
dev = ["pytest>=8.0.0"]
