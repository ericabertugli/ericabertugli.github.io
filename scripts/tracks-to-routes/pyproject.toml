[project]
name = "tracks-to-routes"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fitparse>=1.2.0",
    "h3>=3.7.0",
]

[dependency-groups]
dev = ["pytest>=8.0.0"]

[project.scripts]
fit-to-h3 = "fit_to_h3:main"
